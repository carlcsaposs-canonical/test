[tool.poetry]
name = "foo"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.8"
psycopg2 = [
    {version = ">=2.9.10", python = ">=3.8,<3.9"},
    {version = ">=2.9.11", python = ">=3.9"}
]



[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
